{
  "name": "hrms-saas",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "bootstrap": "npm run build -w @hrms/shared && npm run prisma:generate -w @hrms/server",
    "build": "npm run build --workspaces --if-present",
    "build:shared": "npm run build -w @hrms/shared",
    "dev:server": "npm run start:dev -w @hrms/server",
    "prisma:server": "npm run prisma -w @hrms/server",
    "dev:web": "npm run dev -w @hrms/web",
    "build:web": "npm run build -w @hrms/web",
    "start:all": "npx concurrently \"npm:dev:*\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2",
    "@types/express": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
